`level_1` is the Iris terrain shell for the `Kwietna Polana` dungeon.
It uses a BLUE-channel imagemap (`images/level1_map.png`) to split the footprint into
`dunstart`, `dunmid`, and `dunend`, with lightweight meadow dressing and a flat arena core.
