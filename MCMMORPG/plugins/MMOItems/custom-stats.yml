# Foundation custom stats are intentionally empty for now.
# Add a stat here only after it has a matching entry in language/lore-format.yml
# and language/stats.yml, plus a balance rule in docs/character-sector-contract.md.
