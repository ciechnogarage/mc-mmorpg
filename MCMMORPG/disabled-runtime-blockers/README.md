# Disabled Runtime Blockers

This staging-only folder holds plugin jars or sample configs disabled while
unblocking the foundation MVP baseline.

Do not put secrets, database dumps, private player data, or generated world data
here.

