# Disabled Plugin Jars

Plugin jars here are intentionally disabled for local staging validation.

HuskSync is disabled for the foundation MVP baseline because it failed to load
without a configured database. Production or multi-server staging needs a real
database setup before re-enabling it.

