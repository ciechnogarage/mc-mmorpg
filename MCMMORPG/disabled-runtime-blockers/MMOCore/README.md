# Disabled MMOCore Samples

These files were active sample configs that produced startup warnings during
foundation MVP baseline validation.

They can be restored after they are updated to the installed MMOCore objective
syntax and real staging worlds/regions.

