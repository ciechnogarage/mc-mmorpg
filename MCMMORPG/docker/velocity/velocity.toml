# Velocity — proxy dev MCMMORPG. Jeden punkt wejścia + izolacja backendów.
config-version = "2.8"
bind = "0.0.0.0:25565"
motd = "<gold>MCMMORPG dev</gold> <gray>(velocity)</gray>"
show-max-players = 20
# dev: konta offline / boty walidacyjne
online-mode = false
force-key-authentication = false
prevent-client-proxy-connections = false
# modern forwarding: sekret współdzielony z paper-global.yml (proxies.velocity.secret)
player-info-forwarding-mode = "modern"
forwarding-secret-file = "forwarding.secret"
announce-forge = false
kick-existing-players = false
forced-hosts = {}

[servers]
	# nazwy = DNS kontenerów w sieci mcnet, port wewnętrzny zawsze 25565
	world = "srv-world:25565"
	items = "srv-items:25565"
	hub = "srv-hub:25565"
	# kolejność prób połączenia (on-demand: łączy z tym backendem, który akurat działa)
	try = ["items", "world", "hub"]

[advanced]
	compression-threshold = 256
	compression-level = -1
	login-ratelimit = 3000
	connection-timeout = 5000
	read-timeout = 30000
	tcp-fast-open = false
	proxy-protocol = false

[query]
	enabled = false

[packet-limiter]
	#Size of the moving time window in seconds used to calculate average rates.
	#A larger window tolerates short bursts while still enforcing the configured limits over time.
	interval = 7
	#Maximum average number of packets per second a client may send. -1 disables this check.
	packets-per-second = -1
	#Maximum average number of compressed (on-wire) bytes per second a client may send. -1 disables this check.
	bytes-per-second = -1
	#Maximum average number of decompressed bytes per second a client may send.
	#Protects against compression bomb attacks where small packets expand to excessive sizes after decompression.
	#-1 disables this check.
	decompressed-bytes-per-second = 5242880
