# Agent Workflows

Use this contract before starting T4 orchestrated, automated, or multi-system
work.

```md
# Agent Workflow Contract

Name:

Purpose:

Trigger:
Manual / PR / issue / CI failure / schedule / incident / other

Inputs:

Allowed tools:

Allowed paths:

Forbidden actions:

Human approval required for:

Verification:

Logging:

Done when:

Rollback:

Risk level:
T0 / T1 / T2 / T3 / T4
```

Default stance: read-only access, least privilege, small iteration limits,
deterministic checks, and human approval before side effects.
